---
epic: 5
covers_epics: [5, 6, 7]
also_covers: "Story 4.2 (revisada)"
date: 2026-09-19
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospectiva — Epics 5, 6 e 7 (com a Story 4.2 revisada)

Um único documento cobre os três epics, por decisão da Isabel (interativo): nasceram do mesmo pivô (`sprint-change-proposal-2026-09-18.md`), foram entregues juntos e compartilham arquivos. Ao finalizar, as três chaves `epic-N-retrospective` são marcadas `done`.

## Epic summary

- **Stories concluídas (todas `done`, `pending_stories` vazio nos 3 epics):** 4.2 (revisada), 5.1, 5.2, 5.3, 5.4, 6.1, 6.2, 7.1, 7.2. Nenhuma história não concluída — sem override necessário.
- **Faixa de diff:** `adc8f24^..1472d92` (9 commits, 0 merges) — fonte: `git_evidence.py`, `commit_count: 9`, `merge_count: 0`.
- **Atribuição por commit** (fonte: `git_evidence.py`, campo `stories`): só **3 de 9** commits citam uma story pelo id — `adc8f24` (5.1), `d8e10b8` (5.4), `1472d92` (6.1). `c9e3cf6` ("Epics 4-7") entrega 5.2, 5.3, 5.4 (integração), 6.1, 6.2, 7.1, 7.2 e 4.2 num só commit; por isso **não há diff por story** para 5.2, 5.3, 6.2, 7.1, 7.2 e 4.2.
- **Inventário de evidência**

| Item | Estado | Fonte |
|---|---|---|
| Spec do epic + ACs | Disponível (declarados) | `_bmad-output/planning-artifacts/epics.md` L393–588 |
| Proposta de mudança | Disponível | `planning-artifacts/sprint-change-proposal-2026-09-18.md` |
| Arquivos de spec por story | **Só a 5.1** | `implementation-artifacts/spec-5-1-…md`; `epic-5-context.md` |
| Diff / commits | Disponível, com a ressalva de atribuição acima | `git_evidence.py` |
| Sprint status | Disponível | `sprint-status.yaml` |
| Retro anterior | Disponível (Epics 1–4) | `epic-{1,2,3,4}-retro-*.md`, `action_items` |
| Trabalho adiado | Disponível | `deferred-work.md` |
| Logs de sessão | **Não disponíveis como arquivo** — a análise de lições de processo usa só o histórico desta conversa | — |
| Sub-agentes para as visões agregadas | Não usados — visões calculadas inline (escopo abaixo) | — |

**Escopo estreitado, registrado:** visões agregadas calculadas inline por `wc`/`grep`/leitura dos arquivos, não por ferramenta de grafo de dependências (dependency-cruiser/madge não instalados). O passe `bmad-review` **não** foi executado; em seu lugar vale o `/code-review` rodado sobre a mesma working tree em 2026-09-19 (6 achados; 5 corrigidos em `4c66d0c`/`d8e10b8`/`c9e3cf6`, o 6º em `05ff345`).

## Findings

Cada achado tem duas disposições: **desta instância** (corrigir agora / adiar / aceitar) e **o que evitaria o próximo** (lição a montante).

### Visões agregadas

**F1 — Rastreabilidade: 6 stories sem spec nem commit próprio** · *processo* · lição principal do epic
- Evidência: `git_evidence.py` (3 de 9 commits com story id; `c9e3cf6` cobre 8 stories); só existe `spec-5-1-…md`; `sprint-status.yaml` marca 5.2–7.2 `done` com um comentário de exceção (L85–90 do arquivo, adicionado em `f60d316`).
- Consequência: não dá para atribuir defeitos/churn a uma story, nem reproduzir "o que a story 6.2 mudou". A retro só consegue julgar essas stories pelo **estado final** contra as ACs (ver Verdict), não pelo processo.
- Desta instância: **aceitar** — comportamento verificado (ver Behavior verification) e 304 testes cobrem as ACs.
- A montante: ao entregar N stories no mesmo conjunto de arquivos, ainda assim (a) commit por story ao fechá-la, ou (b) uma micro-spec por story com ACs e "como verifiquei". O acoplamento real (`selectors`, `useTaskActions`, `DayColumn` reescritos juntos) é o motivo honesto do commit único — mas custou a auditabilidade.

**F2 — Lógica de reindexação de `order` implementada 4 vezes; a 4ª esqueceu a invariante** · *duplicação* · corrigir depois
- Evidência: `selectors.ts:56–62` (`reassignDate`), `selectors.ts:75–79` (`closeOrderGap`), `tasksStorage.ts:165–179` (`migrateFromV1`), `applyRollover.ts:35–47` (nova nesta epic). O `/code-review` desta sessão achou que `applyRollover` original mantinha o `order` antigo e duplicava valores em hoje (corrigido em `d8e10b8`).
- Isto **é exatamente** o risco do item `epic-4-retro-item-18` ("uma 4ª função futura poderia esquecer uma guarda que as outras já aprenderam"). Ao fechar esse item como "obsoleto" em `8a0c337` (na conversa de hoje), o julgamento foi errado: as 3 funções antigas sumiram, mas a preocupação se materializou com outras.
- Desta instância: **adiar** com item de ação (extrair um helper `reindexGroup(tasks, date)` e usá-lo nas 4). **Reabrir** `epic-4-retro-item-18` (proposta na seção de follow-through).
- A montante: quando o retro de um epic anterior prevê "4ª cópia esquece a guarda", só fechar o item se o padrão for eliminado, não se as cópias específicas mudarem.

**F3 — Ciclo de vida da janela: `TaskModal` guarda uma janela fixa; rollover só roda quando `today` muda** · *arquitetura/borda* · corrigir agora (pequeno)
- Evidência: `TaskModal.tsx:74` (`useState(() => getWeekWindow())`, calculada uma vez na montagem); `WeekView.tsx` efeito do rollover depende só de `[today]`; `deferred-work.md` (item da Story 5.1: opção de Dia "obsoleta" no select). Dois efeitos: (i) as opções de Dia de um modal de edição aberto atravessando a meia-noite ficam com a janela antiga; (ii) se o usuário salvar com uma data passada dessa lista, a tarefa fica com `date < today`, fora da janela, e **só o próximo `reload` ou a próxima virada de dia a traz de volta** (rollover não roda de novo).
- **Não reproduzido em execução** — derivado do código (a coluna de amanhã sobrevive à virada porque `key={date}` bate, então o modal continua aberto; o do dia que sai é desmontado — ver `WeekView.tsx` `week.map` com `key={date}`). Confirmar com um teste antes de corrigir.
- Desta instância: **corrigir agora** — passar a janela ao modal (ou recalcular a cada render) e/ou rodar `applyRollover` também quando `state.tasks` muda (barato: devolve a mesma referência quando nada rola).
- A montante: o comentário da spec 5.1 dizia que o item "deve deixar de ser possível quando a 5.4 for implementada" — foi só parcialmente verdade; itens adiados condicionados a uma story futura precisam ser revisitados quando ela fecha.

**F4 — Tag "Sem prioridade" quebra em duas linhas em colunas estreitas** · *UX visual, Story 7.1* · corrigir agora
- Evidência: observado no navegador (2026-09-19, viewport ~1280px, screenshot `ss_43108uv9q`): a pill neutra fica alta de duas linhas ("Sem / prioridade") e engorda o card. `PriorityTag.module.css` `.tag` não tem `white-space: nowrap`; `WeekView.module.css:14` usa `repeat(7, 1fr)` sem `min-width` — as colunas encolhem com a viewport.
- Desta instância: **corrigir agora** (nowrap e/ou rótulo mais curto na variante neutra, decisão de design).
- A montante: ACs visuais precisam de uma viewport de referência (o item deferred da 5.1 sobre largura do cabeçalho de coluna é a mesma classe).

**F5 — Comentários obsoletos contradizem o código** · *divergência de padrão* · corrigir agora (trivial)
- Evidência: `constants/week.ts:4–6` ("sem timer… recalculada a cada render/montagem, sem `useEffect`/estado próprio") e `:59`; `TaskModal.tsx:69` ("sem timer de recálculo (Story 5.3)"). `WeekView.tsx` hoje usa estado + timer de 60s.
- Desta instância: **corrigir agora**.

**F6 — `deferred-work.md` acumula itens de código que já foi removido** · *higiene* · adiar
- Evidência: entradas da Story 4.2 original sobre `.zoneActive`, `PRIORITY_ZONES`/`ZONE_LABELS`, `OptimisticSortingPlugin`/`source.group` — funcionalidade removida pelo Epic 4 revisado; e a entrada da 5.1 sobre largura de cabeçalho, que o screenshot de hoje (~1280px) não contradiz mas também não fecha.
- Desta instância: **adiar** — fazer uma passada de limpeza marcando o que ficou obsoleto.

**F7 — Camadas: `state` importa de `components`** · *architecture delta* · aceitar (pré-existente)
- Evidência: `useTaskActions.ts:6` importa `unregisterDragHandle` de `components/WeekView/dragHandleRegistry`. **Já existia antes do epic** (`git show adc8f24^:src/state/useTaskActions.ts`, L5) — não foi introduzido aqui; nenhuma dependência nova de `state`→`components` foi criada nas 9 commits.
- Desta instância: **aceitar**; se um dia mexer, mover o registro para um módulo neutro ou fazer o `WeekView` limpar a alça ao ver a tarefa sumir.
- Sem ciclos novos: `constants/week.ts` continua folha, `storage` só importa `constants` e `types`.

**F8 — Tamanho** · *crescimento de arquivo* · aceitar
- `git_evidence.py` + `wc -l` (antes → agora): `TaskModal.tsx` 422→456, `DayColumn.tsx` 290→169 (−121, remoção das zonas), `WeekView.tsx` 133→155, `useTaskActions.ts` 206→257, `tasksStorage.ts` 95→248 (majoritariamente comentário + migração/validação). Nenhum arquivo cresceu de forma alarmante **neste** epic; `TaskModal.tsx` (456) é o maior e mistura formulário, confirmação de exclusão e focus trap — candidato a divisão, sem pressa.

**F9 — Reconciliação spec ↔ implementação**

| # | Divergência | Fonte | Classe | Proposta |
|---|---|---|---|---|
| a | AC da 4.2 diz "usa `@dnd-kit/react`"; a implementação usa `@dnd-kit/core` (`useDraggable`/`useDroppable`) | `epics.md` L418; `WeekView.tsx` imports; `package.json` | Desvio aceito | Reconciliar o texto da AC (a migração de lib foi feita antes do pivô, commit `41f7f1f`) |
| b | Horário visível no card + no `aria-label` | `TaskCard.tsx` (commit `1472d92`) | Adição não especificada | Acrescentar AC à 6.1 |
| c | Falha de escrita no rollover mostra aviso `role="alert"` e repete a cada 60s | `WeekView.tsx` (`c9e3cf6`) | Adição não especificada | Acrescentar AC à 5.4 (a AC atual só exige "nenhuma tarefa muda de data") |
| d | `time` só aceita `HH:mm`; ids duplicados e ano < 100 tratados na carga | `tasksStorage.ts` (`4c66d0c`, `8443c0d`) | Endurecimento | Registrar em 5.1/6.1 como restrição de dados |
| e | Story 4.1: "o código deve ser removido, não mantido como código morto" | `epics.md` L403; `grep` por `reorderWithinGroup/reorderGroupByIndex/PriorityZone/groupKey/reorderTask` = 0 resultados no `src/` (só `DayOfWeek` para a migração v1) | **Cumprido** | — |

### Lentes de código

Não rodei `bmad-review`; a cobertura de revisão de código vem do `/code-review` desta sessão (fora da skill). Resumo dos 6 achados dele e o destino de cada um:

| Achado do `/code-review` | Estado |
|---|---|
| Rollover duplicava `order` | Corrigido `d8e10b8` (mas veja F2) |
| Falha de escrita do rollover silenciosa | Corrigido em `c9e3cf6` (aviso + retry) |
| `time` sem validação | Corrigido `4c66d0c` |
| Ano < 100 descartava todos os dados | Corrigido `4c66d0c` |
| Modal de edição "regravaria a data antiga" na virada do dia | **Não reproduzível como descrito** (teste descartável: o modal é desmontado junto com a coluna e a tarefa rola para o novo hoje). Um efeito **residual** diferente foi levantado em F3 |
| Foco preso após soltar no mesmo dia | Corrigido `05ff345` com teste que falha sem a correção |

### Lacunas de verificação

- **Arraste com gesto físico real** só foi verificado manualmente pela Isabel em 2026-09-19 ("O arraste está perfeito", na conversa) — sem registro em arquivo e sem saber se foi por mouse, teclado ou ambos; a cobertura automatizada é o `onDragEnd` real com evento sintético (`WeekView.drag.test.tsx`), mais as funções puras (`dragChange.test.ts`). Aceito; recomendo registrar a verificação manual (data + o que foi testado) no `sprint-status.yaml` ou na spec da 4.2.
- **Virada de meia-noite real** só em jsdom com timers falsos (`WeekView.test.tsx` L250–276); não exercida numa aba deixada aberta. Aceito.
- **AC da 4.2 "Card volta ao dia original se a escrita falha"**: coberta no nível da ação (`useTaskActions.test.ts`, `moveTaskToDate` "escrita falha", ~L696) e da camada `dragChange.test.ts:74`; **sem teste de UI** que mostre o card visualmente de volta. Lacuna menor.

## Behavior verification

Exercitado em navegador real (Chrome, dev server Vite, 2026-09-19), hoje = sábado 19/09:

| Fluxo | Resultado observado |
|---|---|
| Janela dinâmica hoje..hoje+6 (5.2) | 19/09 a 25/09, hoje destacado ✅ |
| Criar tarefa com horário e prioridade (6.1) | Salvou `09:30`, prioridade escolhida ✅ |
| Ordenação: sem horário primeiro, depois crescente (6.2) | Confere; horário 09:30 ficou depois das sem horário ✅ |
| Tag cicla ao clicar, sem abrir modal nem mudar estado (7.2) | Baixa → Média ✅ |
| Arraste entre dias (4.2) | **Isabel confirmou manualmente: "perfeito"** ✅ (minhas 2 tentativas automatizadas não moveram o card — limitação do arraste sintético da ferramenta) |
| Horário visível no card (6.1 estendida) | Aparece sobre o título, cinza secundário ✅ |
| **Rollover ao carregar (5.4)** | Semeado: 2 tarefas em hoje (order 0,1), 2 atrasadas não concluídas, 1 atrasada concluída. Resultado: as 2 atrasadas em hoje com order 2 e 3 (depois das existentes), a concluída **ficou em 10/09**, título/horário/estado preservados, ordenação por horário correta, **nenhum aviso** ✅ |
| **Migração v1→v2 (5.1)** | Semeado `schemaVersion: 1` (2× `mon`, 1× `fri`). Resultado: `mon`→21/09 e `fri`→25/09 (dentro da janela, nunca no passado), order renumerado 0,1, `schemaVersion` regravado como 2, sem `loadError` ✅ |
| Tag neutra "Sem prioridade" em coluna estreita (7.1) | ❌ **Quebra em duas linhas** — F4 |

**Não exercitado:** virada de meia-noite real com o app aberto; falha real de `localStorage` (cota) no navegador; leitor de tela; viewport estreita de fato (mobile).

Os dados semeados ficaram no `localStorage` do navegador em `localhost:5173` (dados de teste).

## Previous-retro follow-through

Retros anteriores existem (Epics 1–4). Itens de `action_items` de epics anteriores **ainda não `done`** (após o fechamento de 11 itens em `8a0c337`):

| Item (id exato) | Landed? | Evidência | Status que argumenta |
|---|---|---|---|
| `epic-4-retro-item-14-deferred-work-md-pointersensor-do-dragdr` | Não | `WeekView.tsx:62` `useSensor(PointerSensor)` sem `activationConstraint`; alça de arraste dedicada e verificação manual positiva reduzem o risco | nada (manter `open`, baixa prioridade) |
| `epic-4-retro-item-16-deferred-work-md-arraste-interrompido-pe` | Sem evidência | Descrito para o antigo `DragDropProvider`; hoje `isDragging` vem de `useDraggable` (`DayColumn.tsx:37`), estado da lib — sem teste nem verificação de cancelamento | nada (manter `open`) |
| `epic-4-retro-item-17-deferred-work-md-draghandleregistry-e-um` | Não | `dragHandleRegistry.ts:44` `Map` module-scope inalterado; jsdom cria ambiente por arquivo, então inofensivo | nada (manter `open`) |
| `epic-4-retro-item-18-deferred-work-md-as-3-funcoes-de-reindex` | **Materializou-se** | F2 acima: 4 cópias da reindexação; a 4ª esqueceu a invariante (`d8e10b8`). Hoje está `done` (fechado como obsoleto em `8a0c337`) | **reabrir (`open`)** — proposta, exige sua confirmação |
| `epic-1-retro-item-20-fortalecer-as-checagens-ad-8-ad-9-de-arc` | Não | `git diff adc8f24^..HEAD -- src/architecture.test.ts` vazio | nada (manter `open`) |
| `epic-1-retro-item-21-adicionar-um-teste-que-realmente-prove-q` | Não | nenhum teste novo distinguindo a escrita síncrona de `data-theme`; `ThemeContext.tsx` só ganhou `useLayoutEffect` (item 22) | nada (manter `open`) |
| `epic-1-retro-item-23-deferred-work-md-nenhum-dos-dois-dominio` | Não | `grep 'storage'` por listener de evento em `src/` (sem testes) = 0 resultados | nada (manter `open`) |
| `epic-1-retro-item-26-tratar-testes-auto-aplicaveis-de-arquite` | Parcial | Processo: `WeekView.drag.test.tsx` foi validado com mutação (falha sem a correção), mas `architecture.test.ts` segue sem escopo documentado | nada (manter `open`) |

Nota sobre os itens que eu mesmo fechei hoje: `e2-4`, `e3-8`, `e4-12`, `e4-19`, `e1-22`, `e1-25` têm código/teste como evidência (commits `8443c0d`, `8a0c337`, `05ff345`); `e4-13`, `e4-15` (zonas de prioridade removidas), `e3-9` (não há predicado duplicado: `TaskCard.tsx:61` é o único `=== 'done'`) e `e1-24` (resolvido pela 5.3) foram fechados como obsoletos. Só o `e4-18` foi um julgamento incorreto (F2).

## Action items

Propostos — nenhum aplicado pela retro. Dono `dev` = ciclo de desenvolvimento normal; `process` = mudança de forma de trabalhar; `spec` = Isabel/PM aplica em `epics.md`.

| # | Item | Tipo | Dono |
|---|---|---|---|
| A1 | Extrair um helper único de reindexação por data (`order` 0..n-1) e usar em `reassignDate`, `closeOrderGap`, `migrateFromV1`, `applyRollover` (F2) | Remediação | dev |
| A2 | Alinhar o ciclo de vida da janela: passar a janela ao `TaskModal` (ou recalcular por render) e/ou rodar `applyRollover` também quando `state.tasks` mudar; escrever antes um teste que reproduza a data passada salva a partir de um modal aberto na virada (F3) | Remediação | dev |
| A3 | `white-space: nowrap` (ou rótulo mais curto) na variante neutra da `PriorityTag`, e definir uma viewport de referência para ACs visuais (F4) | Remediação | dev |
| A4 | Corrigir os comentários obsoletos em `constants/week.ts:4–6,59` e `TaskModal.tsx:69` (F5) | Remediação | dev |
| A5 | Limpar `deferred-work.md`: marcar obsoletas as entradas de zonas/`OptimisticSortingPlugin` e reavaliar a da largura do cabeçalho (F6) | Higiene | dev |
| A6 | Reconciliar `epics.md`: 4.2 (`@dnd-kit/core` em vez de `@dnd-kit/react`), 6.1 (horário no card), 5.4 (aviso e retry), restrições de dados de 5.1/6.1 (F9 a–d) | Reconciliação de spec | spec |
| A7 | Ao entregar várias stories no mesmo conjunto de arquivos: commit por story ou micro-spec com ACs e "como verifiquei" (F1) | Processo | process |
| A8 | Ao fechar um item de retro como obsoleto, exigir que o padrão de risco descrito (não só o código citado) tenha sido eliminado (F2) | Processo | process |
| A9 | Registrar a verificação manual do arraste (data, o que foi testado) no `sprint-status.yaml` ou na spec da 4.2 (Lacunas de verificação) | Registro | dev |

## Acceptance verdict

**accepted-with-open-items** — critérios **declarados** (`epics.md` L393–588).

Evidência por story contra as ACs (estado final; ver F1 para a limitação de rastreabilidade):
- **4.2** ✅ mover só a Data (`moveTaskToDate`, `useTaskActions.test.ts` "NUNCA muda Horário/Prioridade/Estado"); via alternativa por teclado presente (`KeyboardSensor`, `WeekView.tsx:63`), mas **não sei qual método a Isabel testou** ao dizer "perfeito" (a minha tentativa automatizada por teclado não moveu o card, por limitação da ferramenta) — o AC do teclado fica sem verificação manual confirmada; guard AD-4 ✔ (`useTaskActions.test.ts:696`).
- **5.1** ✅ spec própria + verificação em navegador real (v1→v2).
- **5.2** ✅ janela hoje..hoje+6, nome + data, destaque de hoje (screenshot).
- **5.3** ✅ timer 60s + rollover junto (`WeekView.test.tsx` L250–276, jsdom; sem teste de meia-noite real).
- **5.4** ✅ rollover direto para hoje, concluída não rola, escrita única (`useTaskActions.applyRollover`), falha não muda estado — verificado em navegador real; comportamento além da AC (retry/aviso) → F9c.
- **6.1** ✅ campo Horário opcional, nunca bloqueia salvar; **6.2** ✅ ordenação por horário, prioridade não influencia, desempate por `order`.
- **7.1** ⚠️ tag sempre visível (AC cumprida), mas com defeito visual em colunas estreitas (F4) — não bloqueante.
- **7.2** ✅ ciclo Sem→Baixa→Média→Alta→Sem, sem abrir modal, acionável por teclado.

**Por que não `accepted`:** ficam achados nomeados e rastreados (F2 duplicação/e4-18 reaberto, F3 borda do ciclo de vida, F4 defeito visual, F1 processo). **Por que não `rejected`:** todas as stories estão `done`, nenhuma AC falha, e nenhum achado bloqueia. Sem override humano necessário.

## Open questions

1. **F3 é real?** A hipótese vem do código, não de execução. Um teste de reprodução (modal de edição aberto → virar o dia → escolher a data antiga) responde; muda a prioridade de A2.
2. **A4/F4 — o que fazer com o rótulo neutro?** `nowrap` deixa a coluna mais apertada; um rótulo curto ("—" ou "Prior.") muda a copy. Decisão de design.
3. **Vale reabrir `epic-4-retro-item-18`** (proposta) ou registrar o helper de reindexação (A1) só como item novo?
